# fonts/

存放桌宠使用的字体文件。

| 文件 | 说明 |
|------|------|
| `XXX.ttf` | 用于全局文字渲染 |

**说明**：程序启动时自动扫描此目录下的所有字体文件（支持 `.ttf`、`.otf`、`.ttc`、`.woff`、`.woff2` 等格式），无需修改代码，放入即可生效。

**渲染范围**：气泡对话、需求状态栏、设置面板、音乐播放器、物品栏等所有界面文字。
